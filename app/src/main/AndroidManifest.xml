<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobile.dev.pickmatkulsqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_round"
        android:label="PILKOM APPS"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CariJadwal.CariJadwalActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".Mahasiswa.MahasiswaActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".Mahasiswa.EditMahasiswaActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".MataKuliah.MataKuliahActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".MataKuliah.EditMataKuliahActivity" android:theme="@style/AppTheme"/>
    </application>

</manifest>